---
all:
  hosts:
    example:
      ansible_host: xxx.xxx.xxx.xxx
      ansible_port: 22
  children:
    element:
      hosts:
        example:
    matrix:
      hosts:
        example:
    postgres:
      hosts:
        example:
