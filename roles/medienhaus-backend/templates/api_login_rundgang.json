{
  "type": "m.login.password",
  "identifier": {
    "type": "m.id.user",
    "user": "{{ medienhaus_backend_rundgang_user_id }}"
  },
  "password": "{{ medienhaus_backend_rundgang_user_password }}"
}
